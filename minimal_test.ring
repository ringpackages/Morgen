# اختبار أساسي جداً لمولد مرجان الذكي

load "morgen/space.ring"

func main {
    see "🧪 اختبار أساسي لمولد مرجان الذكي" + nl
    see "=" * 40 + nl
    
    try {
        # اختبار 1: إنشاء الفضاء المرجاني
        see "1️⃣ اختبار إنشاء الفضاء المرجاني..." + nl
        oSpace = new MorgenSpace(3)
        see "✅ تم إنشاء الفضاء بنجاح!" + nl
        see "   عدد الرموز: " + len(oSpace.aSymbols) + nl + nl
        
        # اختبار 2: إنشاء رمز مرجاني
        see "2️⃣ اختبار إنشاء رمز مرجاني..." + nl
        oSymbol = new MorgenSymbol("test1", "test", [1,2,3,4], 0.5, 45.0, "اختبار")
        see "✅ تم إنشاء الرمز بنجاح!" + nl
        see "   معرف الرمز: " + oSymbol.cId + nl
        see "   نوع الرمز: " + oSymbol.cType + nl + nl
        
        # اختبار 3: إضافة رمز للفضاء
        see "3️⃣ اختبار إضافة رمز للفضاء..." + nl
        oSpace.addSymbol(oSymbol)
        see "✅ تم إضافة الرمز بنجاح!" + nl
        see "   عدد الرموز الآن: " + len(oSpace.aSymbols) + nl + nl
        
        see "🎉 جميع الاختبارات الأساسية نجحت!" + nl
        
    catch
        see "❌ خطأ في الاختبار: " + cCatchError + nl
    }
}

# تشغيل الاختبار
main()
